<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <!--here we are adding the connection string for the DLL to access SQL Server START-->
	<connectionStrings>
		<add name ="Tournaments" connectionString="Data Source=(localdb)\MSSQLLocalDB;Initial Catalog=Tournaments;Integrated Security=True" providerName="System.Data.SqlClient"/>
	</connectionStrings>
	<!--here we are adding the connection string for the DLL to access SQL Server END-->

	<!--here we are adding the connection string for the DLL to access Text File START-->
	<appSettings>
		<add key ="filePath" value="C:\TournamentTracker_CSharp\TournamentTracker\Data"/>
	</appSettings>
	<!--here we are adding the connection string for the DLL to access Text File END-->
	
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.7.2" />
    </startup>
</configuration>